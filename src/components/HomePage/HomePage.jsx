export const HomePage = ({ fetchedMovies }) => {
  return (
    <div></div>
    // <ul>
    //   {fetchedMovies.map(movie => (
    //     <li>{movie.title}</li>
    //   ))}
    //   ;
    // </ul>
  );
};
